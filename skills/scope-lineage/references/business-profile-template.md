# 任务画像：{task_name}

> 空白模板。按 `references/semantic-profile-prompt.md` 填写；`{…}` 是占位符，填完必须全部消失。
> 正文（一、二、三）不出现来源标签与结构词；句末只允许 `[推断]` / `[待确认]`，事实句不标。
> 例外：第三件每条的 `- 证据：` 行写的就是骨架路径，结构词照写，不计入自检第 4 项。
> 生成记录（输入文件校验、来源标签与证据、结构推断项、自洽性检查、生成自检）不写在本文件里，
> 按 `references/business-profile-check-template.md` 另写 `business_profile.check.md`。
> 本行与上面四行在成稿里删掉。

## 一、任务语义卡

<!-- 上限 = 600 + 40 × max(0, 输入表数 − 3)，封顶 900 字（中文字符，不含表格）。 -->

### 这个任务做什么

{2–4 句业务语言：解决什么问题、产出什么、给谁用。纯归纳的那句标 [推断]。}

### 一行代表什么

- 粒度：{一行 = {业务对象} × {维度}，来自 output_shape.grain 的逻辑键}
- 键：{目标表的哪几列能唯一标识一行；按 key_confidence 措辞：确实唯一 / 唯一性依赖的列没写进表 / 只是候选 [待确认] / 没有}
- 分区列：{列名｜无}（不是业务主键）

### 数据从哪来、到哪去

- {输入表 1}：{是什么}，在这里{起什么作用}。
- {输入表 2}：{…}
- 下游：{消费它的任务或人｜下游未知}

### 口径要点

1. 本实例取数日 {task.instance_dates}（每次运行按实例日期替换，不是 SQL 固定日期）{；<另一侧> 取前 N 日}｜取数日由调度变量替换
2. {时间口径：当天 / T-1 / 近 N 天}
3. {核心指标的分子与分母}
4. {更新方式：覆盖写 / 追加，调度频率}
5. {…最多 6 条}

### 使用注意

<!-- 只写 confidence.findings[].severity = warn 的那些。实例日期、两侧取不同天、缺表注释是
     severity = info，不写在这里，也不生成 Q。 -->

- {读者必须知道的一条，一句话}
- {列错位 / 关联不上时为空 / UDF 黑盒 / 追溯不完整 …}
- {无则整节写「无」}

## 二、字段字典

共 {M} 个输出字段，全部列出。

| # | 字段 | 中文名 | 一句含义 | 口径 | 取值含义 | 可信度 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `{column}` | {注释｜推断名?} | {是什么} | {对象 · 时间范围 · 纳入条件 · 聚合｜取自 <表>.<列>} | {取值 = 含义｜候选：…｜含义待确认，见附录 B｜—} | {事实｜推断｜待确认} |
| 2 | | | | | | |

> 字段 > 40 时改为五张分组表：键 / 时间 / 指标 / 标签 / 属性，每张表后写「本组 N 个字段」，
> 各组之和 = {M}，在生成记录的自检第 6 项核对。

### 口径卡：`{metric_column}`{（覆盖 {同口径字段区间}）}

| 槽位 | 值 |
| --- | --- |
| 统计对象 | {metric_spec.subject｜未知} |
| 时间范围 | {metric_spec.time_range｜未知} |
| 纳入条件 | {metric_spec.inclusion｜未知} |
| 聚合方式与粒度 | {metric_spec.aggregation｜未知} |
| 单位/类型 | {metric_spec.unit｜未知} |
| 空值处理 | {metric_spec.null_handling｜未知} |
| 更新频率 | {metric_spec.refresh｜未知} |
| 聚合后加工 | {metric_spec.post_aggregation——为空时删掉本行} |

> 每个指标字段一张；降级拼出来的行在行末标 `[推断]`。

## 三、待确认清单

共 {N} 条（**≤ 5**），按影响排序，不标「优先」——五条都是优先。只问四类：列错位 /
注释与推导链语义冲突 / 关联可能放大且键未证明（合成一条）/ 名不副实字段。
取值含义不在这里问：见附录 B。四类都不成立时整节写「本轮无需确认」。

Q1. {一句问题}
- 证据：{骨架路径或注释原文——这一行允许写结构词}
- 候选答案：{A / B / C｜无候选}
- 回写目标：{四选一，只写一种：字段注释:<表.列>｜术语:<词>｜值域:<列>=<值>｜表注释:<表>}
- 影响：{答错会怎样}
- 答案：（待填）

Q2. {…}

> 业务方把答案写在每条的 `- 答案：` 行，然后跑
> `python3 scripts/confirmations.py apply <本文件> --by <名字>`：`术语` / `值域` 进
> `glossary.overrides.json`，`字段注释` / `表注释` 进 `metadata-patch.json`，下一轮画像里
> 这些项就不再提问。已确认的项不写在这里，写在附录 A。


## 附录：给读者的三张表

<!-- 附录只留读者用得上的三节，合计字数 ≤ 正文（一、二、三）字数的 1/3。
     输入文件校验、来源标签与证据、结构推断项、自洽性检查、生成自检是写作方的质检记录，
     不写在这里，写进 `business_profile.check.md`。 -->

### 附录 A 已确认项

| 类型 | 对象 | 确认后的含义 | 证据 |
| --- | --- | --- | --- |
| {取值｜字段注释｜表注释} | {<列>='<值>'｜<表.列>｜<表>} | {} | {confirmed｜patch} |

### 附录 B 备查项与待填取值

超出 5 条的候选，一行一条：

- {一句话} —— 证据 {骨架路径}

取值含义请填 `glossary.overrides.template.md`，生成命令：
`scope-lineage glossary --lineage <语料> --out <目录> --template <目录>/glossary.overrides.template.md`

| 列 | 待填取值 | 个数 | 封闭 |
| --- | --- | --- | --- |
| `{表.列}` | `{'A'}`、`{'B'}` | {N} | {是｜未证明} |

### 附录 C 风险边界

| 风险项 | 来源 | 状态 | 说明 |
| --- | --- | --- | --- |
| trace_complete=false 字段 | semantic.json.confidence | {} | {} |
| diagnostics warning | diagnostics.json | {} | {} |
| metadata 缺失 | confidence / diagnostics | {} | {} |
| 结构推断项（形态/粒度/窗口意图/放大风险） | semantic.json | {} | 非业务定义 |
| SELECT * / JSON / UDF 黑盒 | diagnostics / stages | {} | {} |

#### 列错位（alias_position_mismatch）

{无则写「无」。有则：N/M 个投影别名与目标表同位置列名不同；前 3 例 `目标 a ← 别名 b`；
证据 mapping_chain_id；动作建议：请 DESC 目标表核对列序；对应 Q{n}。}

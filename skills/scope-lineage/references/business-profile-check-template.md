# 画像生成记录：{task_name}

> 空白模板。与 `business_profile.md` 同目录、同时生成，文件名 `business_profile.check.md`；
> 按 `references/semantic-profile-prompt.md` 填写，`{…}` 是占位符，填完必须全部消失。
> 这是写作方的质检记录，不是给业务方读的文档：正文（一、二、三）里的每一句要能从这里追回契约 id。
> 自检表仍然管的是 `business_profile.md` 的正文，不是本文件。
> 允许长，不计入画像附录 ≤ 正文 1/3 的上限。
> 本行与上面五行在成稿里删掉。

## 一、输入文件校验

| 文件 | 是否读取 | 用途 | 关键校验结果 |
| --- | --- | --- | --- |
| semantic.md | {是/否} | 事实骨架 | {字段数 / 阶段数 / 规则数} |
| semantic.json | {是/否/定向取} | 结构化字段 | {取了哪些路径；metric_spec 是否存在} |
| diagnostics.json | {是/否} | warning、断链、风险 | {analysis_status、警告条数} |
| mapping.md | {是/否/未必要} | 技术细节补充 | {} |
| lineage.json | {是/否/未必要} | query.py 定向取 | {取了哪些字段} |
| 原始任务 JSON | {是/否} | SQL 注释、任务元信息 | {注释条数、元信息键} |

## 二、来源标签与证据

| 标签 | 正文位置 | 内容 | 证据 id |
| --- | --- | --- | --- |
| `SQL事实` | {一 · 口径要点 1} | {} | {logic:…} |
| `元数据事实` | {二 · 中文名列} | {} | {表.列} |
| `结构推断` | {一 · 一行代表什么} | {} | {scope_id / mc:…} |
| `SQL注释` | {一 · 这个任务做什么} | {} | {注释原句} |
| `任务元信息` | {一 · 到哪去} | {} | {meta 键名} |
| `LLM推断` | {正文 [推断] 句} | {共 N 句} | — |
| `待业务确认` | {正文 [待确认] 句} | {共 N 句} | {Q1、Q3…} |

已确认：取值 {N1}、术语 {N2}、字段注释 {N3}、表注释 {N4}（转写 `confidence.confirmations`）。
取值含义覆盖率（`confidence.metadata_coverage.glossary`）：{已确认 confirmed / enumerable_total}。

## 三、结构推断项

- 骨架推断项（`confidence.inferred_items` 按路径计数）：{"fields[].structural_role": N, …}
- 本文档新增：`LLM推断` {N} 句、`待业务确认` {N} 句

## 四、自洽性检查

自洽性检查：{N} 字段、{0} 冲突{；冲突逐条：字段 / 两处说法 / 保留哪个 / 对应 Q 编号}

交叉校验（`diagnostics.json` 与 `semantic.json.confidence`）：{一致｜不一致，分别列出两个来源的值}

## 五、生成自检

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 1. 实际读取 semantic.md | {✅/⚠️/❌} | {} |
| 2. 实际读取 diagnostics.json | | |
| 3. semantic.json 按路径定向取，未整读 lineage.json | | |
| 4. 语义卡 ≤ `600 + 40 ×（输入表数 − 3）`（封顶 900），正文单元格无结构词与来源标签（`- 证据：` 行豁免） | | |
| 5. 只在推断与待确认句上标了记号，事实句无记号 | | |
| 6. 字段字典覆盖全部输出字段（{N}/{M}） | | |
| 7. 每个指标字段都有 7 行口径卡，降级行已标 `[推断]` | | |
| 8. 「一行代表什么」按 key_confidence 措辞，proven_unexposed 进了使用注意 | | |
| 9. 区分了「直接读取物理表」与「上游可追溯」 | | |
| 10. 逐字段核对了注释与 derivation 冲突，冲突的进了清单 | | |
| 11. alias_position_mismatch（若有）在附录 C 单独成段 | | |
| 12. 待确认清单 ≤ 5 条且只在四类之内，六行格式齐全（含 `- 答案：（待填）`） | | |
| 12a. 每条「回写目标」只写一种形式；已确认项没有再提问，都在附录 A | | |
| 12c. severity = info 的 findings 没有进正文，也没有生成 Q | | |
| 12d. 超额候选与待填取值都在附录 B | | |
| 13. 自洽性检查已执行并写出结论 | | |
| 14. 没有编造骨架中不存在的表、字段、规则 | | |

附录占比：附录 A+B+C {N} 字 / 正文 {M} 字 = {比例}（须 ≤ 1/3；超了就把证据搬回本文件）。
